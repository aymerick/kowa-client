<section class="content-master">
    <header class="content-header">
        <div class="content-kind-title">{{t 'Events'}}</div>
        {{#link-to "events.new" class="content-add btn btn-success btn-xs" title=(t 'event.new')}}<i class="fa fa-plus"></i> {{t 'event.new'}}{{/link-to}}
    </header>
    {{#view "paginated-scroll-box" tagName="ol" classNames="content-body"}}
        {{#each controller itemView="list-item-view" as |item|}}
            {{#link-to "events.event" item title=(t 'event.edit')}}
                {{#if item.cover}}
                    <div class="item-cover">
                        <img src={{item.cover.squareUrl}} alt={{t 'coverImage'}} />
                    </div>
                {{/if}}
                <div class="item-infos">
                    <h3 class="item-title">{{item.title}}</h3>
                    <section class="item-meta">
                        <div class="item-place">{{item.place}}</div>
                        <div class="item-date">{{item.dateSentence}}</div>
                    </section>
                </div>
            {{/link-to}}
        {{/each}}
    {{/view}}
</section>
<section class="content-details">
    {{outlet}}
</section>
