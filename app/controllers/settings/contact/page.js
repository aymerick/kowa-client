import Ember from 'ember';
import PageSettingsControllerMixin from 'kowa/mixins/page-settings-controller';

var SettingsContactPageController = Ember.Controller.extend(PageSettingsControllerMixin, {
});

export default SettingsContactPageController;
