import Ember from 'ember';
import PageSettingsControllerMixin from 'kowa/mixins/page-settings-controller';

var SettingsEventsController = Ember.Controller.extend(PageSettingsControllerMixin, {
});

export default SettingsEventsController;
